import React from "react";
import { Box, Typography } from "@mui/material";

const PostTheTopic = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">Đăng Tải Đề Tài</Typography>
      {/* Thêm form hoặc nội dung đăng tải đề tài tại đây */}
    </Box>
  );
};

export default PostTheTopic;
